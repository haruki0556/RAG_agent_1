

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    env_file:
      - .env
    ports:
      - 8000:8000
    command: chainlit run app.py --host 0.0.0.0 --port 8000

